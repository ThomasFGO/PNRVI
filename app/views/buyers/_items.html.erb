<div class="items_visuals-container">
  <% @order_items_by_buyer.each do |order_item| %>
    <%= link_to card_path(order_item.item.itemable) do %>
      <div class="card-element shop-element">
          <div class="visual">
            <%= cl_image_tag order_item.item.ph_one,
            :gravity=>"auto", :crop=>"fill",  :transformation=>[
            {:width=>500, :crop=>"scale"},
            {:overlay=>{:font_family=>"Arial", :font_size=>50, :text=>"#{order_item.item.user.first_name} #{order_item.item.user.last_name}"},
             :gravity=>"south", :y=>100}
            ], class: "mini" %>
          </div>
        <div class="shop-infos">
          <div class="price">
            <%= "#{order_item.item.value} €" %>
          </div>
        </div>
      </div>
    <% end %>
  <% end %>
</div>

