<div class="container">


  <%= simple_form_for [@collection_card, @picture] do |f| %>
    <%= f.input :photo %>
    <%= f.input :photo_cache, as: :hidden %>
    <%= f.submit "Ajouter à ma collection", class: "btn btn-primary" %>
  <% end %>
</div>

