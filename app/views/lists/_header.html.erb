<div class="list-header">
  <div class="list_header-box">
    <div class="first_part">
      <div class="logo">
        <%= cl_image_tag ("lists/fr_logos/poknroll_#{@list.code}_fr_logo.png") %>
      </div>
      <div class="name_and_symbol">
        <div class="name">
          <h4><%= @list.fr_name %></h4>
        </div>
        <div class="symbol">
          <%= cl_image_tag ("lists/symbols/poknroll_#{@list.code}_symbol.png"), class: "symbol-list" %>
        </div>
      </div>
    </div>
    <div class="second_part">
      <div class="size_and_secrets">
        <div class="intro">
          <p>Cette série contient : </p>
        </div>
        <div class="numbers">
          <% if @list.ref_cards.where(rarety_type: "Secret").count > 1 %>
            <p> <%= @list.size %> cartes et <%=  @list.ref_cards.where(rarety_type: "Secret").count %> secrêtes</p>
          <% else %>
            <p> <%= @list.size %> cartes et aucune secrête</p>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
