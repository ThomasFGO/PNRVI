<div class="container">

  <%= render partial: "collection_cards/header" %>


  <!-- Nav tabs -->
  <ul class="nav nav-tabs" id="myTab" role="tablist">
    <li class="nav-item">
      <a class="nav-link active" id="by_list-tab" data-toggle="tab" href="#by_list" role="tab" aria-controls="by_list" aria-selected="true">
        Séries
      </a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="last_photos-tab" data-toggle="tab" href="#last_photos" role="tab" aria-controls="last_photos" aria-selected="false">
        Dernières photos
      </a>
    </li>
  </ul>

  <!-- Tab panes -->
  <div class="tab-content">
    <div class="tab-pane active" id="by_list" role="tabpanel" aria-labelledby="by_list-tab">
      <%= render partial: "collection_cards/by_list" %>
    </div>

    <div class="tab-pane" id="last_photos" role="tabpanel" aria-labelledby="last_photos-tab">
      <%= render partial: "collection_cards/last_photos" %>
    </div>
  </div>
</div>

</div>
